<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NetwokAndLocalDB"
        tools:targetApi="31">

        <!--

         <activity
            android:name=".mvvm_livedata_room.CompanyMainActivity"
            android:exported="false" />

          <activity
            android:name=".mvvm_room_db.StudentMainActivity"
            android:exported="false" />

          <activity
            android:name=".simple_room_db.UserMainActivity"
            android:exported="false" />

             <activity
            android:name=".practice_simple_room_db.EmployeeMainActivity"
            android:exported="false" />
        -->
        <activity
            android:name=".mvvm_livedata_room.CompanyMainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>